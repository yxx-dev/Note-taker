const express = require('express');
const nt = express.Router();
const fs = require('fs');
const {v4: uuidv4} = require('uuid');

//GET route
nt.get('/', (req, res) => {

})

//POST route
nt.post('/', (req, res) => {

})

module.exports = nt;

